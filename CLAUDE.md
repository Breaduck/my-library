@AGENTS.md
Deploy: push to https://github.com/Breaduck/my-library (master) → GitHub Actions가 Cloudflare Pages에 자동 배포. 로컬 빌드: npm run build, 배포: npm run pages:deploy.

# 작업 방식
- 작업 중 확인 질문 없이 모두 yes로 진행한다. 묻지 말고 바로 실행.
